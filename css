/* Importação das fontes */
@import url('https://fonts.googleapis.com/css2?family=Nunito+Sans:wght@400;700&display=swap');
@import url('https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap');

/* Definições de variáveis */
:root {
    --bg-color: #222831;
    --primary-color: #DDDDDD;
    --secondary-color: #F05454;
    --text-color: #000000; /* Cor preta */
    --font-main: "Nunito Sans", sans-serif;
    --font-header: "Roboto", sans-serif;
    --background-image: url('https://codecrush.com.br/static/images/articles/rede-social.png');
}

/* Estilo do body */
body {
    background-color: var(--bg-color);
    color: var(--text-color); /* Aplica a cor preta ao texto */
    font-family: var(--font-main);
    height: 100vh;
    margin: 0;
    background-image: var(--background-image); /* Aplica a imagem de fundo */
    background-size: cover; /* Ajusta a imagem para cobrir todo o fundo */
    background-repeat: no-repeat; /* Evita a repetição da imagem */
    background-position: center; /* Centraliza a imagem de fundo */
}

/* Estilo do header */
header {
    background-color: var(--primary-color);
    color: var(--bg-color);
    text-align: center;
    padding: 1rem;
    font-family: var(--font-header);
}

/* Estilo do h1 */
h1 {
    font-size: 2rem;
    color: var(--text-color); /* Aplica a cor preta ao texto do cabeçalho */
    font-weight: 700;
    margin: 0;
}

/* Estilo do nav */
nav {
    display: flex;
    justify-content: center;
    margin-top: 1rem;
}

nav a {
    text-decoration: none;
    color: var(--text-color); /* Aplica a cor preta aos links */
    margin: 0 1.5rem;
    font-size: 1.2rem;
}

nav a:hover {
    text-decoration: underline;
    transform: scale(0.95);
    transition: transform 0.2s;
}

/* Estilo do footer */
footer {
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: var(--primary-color);
    color: var(--text-color); /* Aplica a cor preta ao texto do rodapé */
    width: 100%;
    height: 3rem;
    position: absolute;
    bottom: 0;
    font-family: var(--font-main);
}
